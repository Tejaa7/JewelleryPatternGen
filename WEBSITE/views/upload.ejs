<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image Upload and Display</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/Users/mukundareddy/Desktop/KMIT/styles.css">
    <style>
                .nav-link {
            font-weight: bold;
            font-size: 1.3rem;
            color: black;
            text-decoration: none;
        }
        .nav-link:hover {
            color: #007bff;
        }
        .logo img {
            height: 100px;
            width: 270px;
        }
        .nav-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 999;
            background-color: white;
            padding: 5px 30px 5px 15px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .nav-container ul {
            display: flex;
            justify-content: space-between;
            padding: 0;
            margin: 0;
            list-style-type: none;
            flex-grow: 1;
        }
        .nav-item {
            flex-grow: 1;
            text-align: center;
        }
        .nav-item a {
            display: block;
        }
        .logo{
            height: 100px;
            width: auto;
            background-size: cover;
            border-radius: 10px;
            display: flex;
            background-position: center;
            justify-content: center;
        }
        .logo img{
          height: 100px;
        }
        body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}
        body {
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            /* min-height: 100vh; */
            background-color: #f1ebe5;
        }

        .container {
            display: flex;
            justify-content:center;
            align-items:baseline;
            /* border-top: 30px solid #f1ebe5;
            border-left: 100px solid #f1ebe5;
            border-right: 100px solid #f1ebe5;
            border-bottom: 60px solid #f1ebe5; */
            padding-top: 30px;
            gap: 50px;
            width: 100%; 
            box-sizing: border-box; 
        }

        .leftbox, .rightbox {
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            justify-content: flex-start;
        }

        #imagePreviewContainer1, #imagePreviewContainer2 {
            width: 400px;
            height: 400px;
            border: 2px solid black;
            border-radius: 10px;
            background-color:#f1f1f1;
            padding: 10px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.2);
            margin-top: 20px;
        }

        #imagePreview {
            width: 100%;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        #imagePreview img {
            max-width: 100%;
            max-height: 100%;
            display: none;
            border-radius: 5px;
        }

        footer {
            background-color: white;
            color: #fff;
            width: 100%;
            /* position: fixed; */
            text-align: center;
            padding: 50px 0;
            box-sizing: border-box;
            /* margin-top: 50px; */
            /* bottom: 0;
            left: 0;
            padding: 20px;
            text-align: center; */
        }
        .right, .left {
            width: 90%;
            padding: 0.75rem;
            color: black;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 1rem;
            font-family: 'Times New Roman', Times, serif;
            font-weight: 400;
            display: flex;
            align-items: center;
            transition: background-color 0.3s ease;
        }
        .right{
            padding-left: 80px; 
        }
        .left{
            padding-left: 80px;
        }
        .upload-label {
            font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
            font-size: 29px;
            font-weight: 600;
            color: black;
            margin-bottom: 10px;
        }
        .arrow {
            display: flex;
            align-items:center;
            font-size: 55px;
            color:#333;
            justify-content:center;
        }
        .hero-section {
            display: flex;
            justify-content: left;
            align-items: center;
            margin-top: 110px; 
            background-color: #f1ebe5;
            gap: 20px; 
            /* padding-top: 10px; */
            /* padding-bottom: 10px; */
            height:72vh;
        }

        .hero-section video {
            width: 60vw;
            height: 74vh; 
            object-fit: cover;
        }

        .hero-section p {
            font-size: 3rem; 
            text-align: center;
            margin: 0;
            max-width: 50vw;
            color: #333;
        }
        .Inline{
            padding-left: 150px;
            font-family:Georgia, 'Times New Roman', Times, serif;
            font-weight:550;
            background-color: #f1ebe5;
        }
        .hidden-on-scroll{
        opacity: 0; 
        transform: translateY(20px); 
        transition: opacity 1s ease, transform 1s ease; 
}

        .hidden-on-scroll.show{
        opacity: 1; 
        transform: translateY(0);
}
    </style>
</head>
<body> 
    <div class="nav-container">
        <div class="logo" >
            <a href="/Home"><img src="/images/LOGO1.jpg" alt="Logo"></a>
        </div>
        <ul class="nav">
            <li class="nav-item"><a class="nav-link" href="/Home">Home</a></li>
            <li class="nav-item"><a class="nav-link" href="/traditional">Traditional</a></li>
            <li class="nav-item"><a class="nav-link" href="/modern">Modern</a></li>
            <li class="nav-item"><a class="nav-link" href="/upload">Upload</a></li>
            <li class="nav-item"><a class="nav-link" href="/contactus">Contact Us</a></li>
            <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
        </ul>
    </div>
    <section class="hero-section">
        <video autoplay muted loop>
            <source src="/images/uploadvideo.mp4" type="video/mp4">
        </video>
        <p class=" Inline">
            Paper to Perfection
        </p>
    </section>
    <div class="hidden-on-scroll"></div>
    <footer>
        <div class="container">
            <div class="leftbox">
                <label for="imageUpload1" class="upload-label">Upload Your Sketch</label>
                <input type="file" class="left" id="imageUpload1" accept="image/*" />
                <div id="imagePreviewContainer1">
                    <div id="imagePreview">
                        <img id="previewImg1" src="" alt="Image Preview">
                    </div>
                </div>
            </div>

            <div class="arrow">→</div>

            <div class="rightbox">
                <label for="imageUpload2" class="upload-label">Your Final Product</label>
                <input type="file" class="right" id="imageUpload2" accept="image/*" />
                <div id="imagePreviewContainer2">
                    <div id="imagePreview">
                        <img id="previewImg2" src="" alt="Image Preview">
                    </div>
                </div>
            </div>
        </div>
    </footer>
</div>

    <script>
        document.getElementById('imageUpload1').addEventListener('change', function(event) {
            const file = event.target.files[0];
            const reader = new FileReader();

            reader.onload = function(e) {
                const previewImg = document.getElementById('previewImg1');
                previewImg.src = e.target.result;
                previewImg.style.display = 'block';
            }

            if (file) {
                reader.readAsDataURL(file);
            }
        });

        document.getElementById('imageUpload2').addEventListener('change', function(event) {
            const file = event.target.files[0];
            const reader = new FileReader();

            reader.onload = function(e) {
                const previewImg = document.getElementById('previewImg2');
                previewImg.src = e.target.result;
                previewImg.style.display = 'block';
            }

            if (file) {
                reader.readAsDataURL(file);
            }
        });
    </script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const elements = document.querySelectorAll('.hidden-on-scroll');
    
            const observerOptions = {
                threshold: 0.1 
            };
    
            const observer = new IntersectionObserver(function(entries) {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
    console.log("Element is visible:", entry.target); 
    setTimeout(() => {
        entry.target.classList.add('show');
        observer.unobserve(entry.target);
    }, 500); 
}

                });
            }, observerOptions);
    
            elements.forEach(element => {
                observer.observe(element);
            });
        });
    </script>
</body>
</html>
